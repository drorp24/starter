%h1 Listing general_profiles

%table
  %tr
    %th User
    %th Title
    %th
    %th
    %th

  - @general_profiles.each do |general_profile|
    %tr
      %td= general_profile.user_id
      %td= general_profile.title
      %td= link_to 'Show', general_profile
      %td= link_to 'Edit', edit_general_profile_path(general_profile)
      %td= link_to 'Destroy', general_profile, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New General profile', new_general_profile_path
