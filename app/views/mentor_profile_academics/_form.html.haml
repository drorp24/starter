= form_for @mentor_profile_academic do |f|
  - if @mentor_profile_academic.errors.any?
    #error_explanation
      %h2= "#{pluralize(@mentor_profile_academic.errors.count, "error")} prohibited this mentor_profile_academic from being saved:"
      %ul
        - @mentor_profile_academic.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
